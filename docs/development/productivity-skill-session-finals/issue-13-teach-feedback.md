Correct. You chose the right order and correctly separated false (`1`) from command failure. One precision: treat **any** non-zero status other than `1` as an error; `2` is one example.

Learning evidence warranted updates: [glossary](GLOSSARY.md) and [learning record 0002](learning-records/0002-release-ancestry-decision-demonstrated.md). The mission is unchanged.

Next spaced practice: on **September 16**, without notes, solve two ancestry checks where timestamps are deliberately misleading or branches diverge; state the command and safe handling for each result.