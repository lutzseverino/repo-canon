Updated the [durable Intake batch plan](/tmp/repo-canon-skill-exercises-11/planning/docs/planning/intake-batch-plan.md:1) with the closed public contract and added Carrier reference/Destination postal code terminology to [CONTEXT.md](/tmp/repo-canon-skill-exercises-11/planning/CONTEXT.md:1).

The plan now settles route, JSON shape, `201`/`422` outcomes, complete `{ index, field, code }` errors, duplicate semantics, concurrent unique-constraint translation, and unchanged compatibility policies. No specification decision remains unresolved.

Validated with `npm test` and `git diff --check`. No implementation or remote publishing occurred.

Committed:

`372dedf docs: finalize intake batch contract`
