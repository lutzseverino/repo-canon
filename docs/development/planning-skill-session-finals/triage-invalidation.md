Invalidated receipt-export readiness and committed it.

- Updated [01-export-receipts.md](/tmp/repo-canon-skill-exercises-11/triage-wayfinder/.scratch/triage/01-export-receipts.md:3) to `needs-triage`.
- Reconciled the fourth `delivery status` column into the Agent Brief without adding behavior beyond the reporter’s clarification.
- Recorded that renewed maintainer semantic review is required before readiness returns.
- Committed: `a36df15 chore(triage): invalidate receipt export readiness`
- Verified `git diff --check` and `npm test` (0 tests, passing).
