Resolved the conflict in `src/order-intake.mjs`, preserving both accepted requirements. `npm test` passes (3/3).

I could not stage or commit: this session mounts `.git` read-only, and `git add` fails creating `.git/index.lock`. The merge remains in progress with the resolved file unstaged.
