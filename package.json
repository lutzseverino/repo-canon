{
  "name": "repo-canon",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "check": "node --check .github/scripts/validate-pr-metadata.mjs && node --check test/pr-metadata.test.mjs",
    "test": "node --test test/pr-metadata.test.mjs"
  }
}
